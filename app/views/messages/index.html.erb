<h1>Messages</h1>

<ul>
  <% @messages.each do |message| %>
    <li><%= link_to "#{message.id} #{message.body}", message_path(message) %></li>
  <% end %>
  <span id="message-list"></span>
</ul>

<%= link_to "New message", new_message_path, id: "new-text-link", remote: true %>
