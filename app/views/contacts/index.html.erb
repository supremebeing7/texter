<h1>Contacts for <%= current_user.email %></h1>

<%= link_to "New Contact", new_contact_path %><br><br>

<h2>Contacts:</h2>

<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, contact_path(contact) %></li>
  <% end %>
</ul>
